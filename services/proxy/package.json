{
  "name": "@cwr/proxy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Minimal server-side proxy for RPC and the Zama relayer: keeps private keys off the client, origin-allowlisted (zero runtime deps).",
  "license": "MIT",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "tsx": "^4.19.2"
  },
  "devDependencies": {
    "@types/node": "^20.19.30",
    "typescript": "^5.9.3"
  }
}
